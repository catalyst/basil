{
  "template_version": "1.1",
  "title": "Basic Django",
  "description": "A template for a basic django installation in a VM. The folder structure is inspired by the best practice guidelines of Two Scoops ♥. One point of departure is the approach taken to separating secrets (unversioned) from settings (versioned).",
  "fields": {
    "project_name": {
      "type": "text",
      "title": "Project name",
      "default": "",
      "description": "The name of the project.",
      "validators": [
        "directory",
        "unique_python_module"
      ]
    },
    "admin": {
      "type": "text",
      "title": "Admin name",
      "default": "admin",
      "description": "The name of the administrator of the Django project.",
      "validators": [
        "username"
      ]
    }
  },
  "port_tags": [
    "webserver_port"
  ]
}
